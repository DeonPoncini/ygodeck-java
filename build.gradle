apply plugin: 'java'

def nativeLibs = fileTree(nativeDir)

sourceSets {
    main {
        java.srcDirs = ['src', javaIncludes]
    }
}

jar {
    archiveName='ygodeck.jar'
    into ("lib/" + archiveNativeDir, { from nativeLibs})
}
